import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.credit.wallet',
  appName: 'Carga de credito',
  webDir: 'www',
};

export default config;
